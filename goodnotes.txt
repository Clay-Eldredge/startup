/characters
- Needs to load nav links for all 89 characters
    - Should have nav link, take user to feed, pass through character as parameter
    - Image of character

/feed
- Needs to load all posts related to selected character
    - Post should have
        - Username
        - Date & time
        - Text content
    - Browser should be able to parse post texts and assign tags ---OR--- just straight up pass tags as a separate paramter
- Needs to mimic posting functionality

/login
- Functionality can probably wait until login assignment

/profile
- Needs to load user data
